[build]
  command = "echo 'resourceDir: /opt/build/cache/resources_cache' >> hugo.yaml && FIPGUIDE_BRANCH=\"$BRANCH\" hugo --gc --minify -b $DEPLOY_PRIME_URL && npx -y pagefind --site public"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.164.0"
